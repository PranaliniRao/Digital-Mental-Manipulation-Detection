import { ArrowRightLeft, Check } from 'lucide-react'
import { Button } from '../../../components/ui'
import { BarChart } from '../components/ChartPrimitives'
import { PageShell, Panel, SectionTitle } from '../components/PageShell'
export function CompareReportsPage() { const cards = [{ title: 'Election claim thread', meta: 'Text analysis · AN-4821', risk: '82', label: 'High risk', values: [35, 55, 47, 81, 72, 88] }, { title: 'Market outlook newsletter', meta: 'Text analysis · AN-4819', risk: '61', label: 'Medium risk', values: [28, 36, 62, 51, 57, 65] }]; return <PageShell title="Compare reports" description="Place two analyses side by side to identify meaningful differences in risk, signal patterns, and review context." action={<Button variant="secondary"><ArrowRightLeft size={16}/>Swap reports</Button>}><div className="grid gap-5 lg:grid-cols-2">{cards.map(({ title, meta, risk, label, values }) => <Panel key={title}><span className="font-mono text-[.61rem] uppercase tracking-[.14em] text-violet">Selected report</span><h2 className="mt-3 text-xl font-semibold">{title}</h2><p className="mt-1 text-sm text-muted">{meta}</p><div className="mt-7 rounded-control border border-subtle bg-canvas/45 p-4"><p className="text-xs text-muted">Overall risk</p><p className="mt-1 text-4xl font-semibold text-violet">{risk}<span className="text-base text-muted">/100</span></p><p className="mt-1 text-xs text-warning">{label}</p></div><SectionTitle title="Signal timeline" description="Placeholder comparison series"/><BarChart values={values}/></Panel>)}</div><div className="mt-5 grid gap-5 md:grid-cols-2"><Panel><SectionTitle title="Agent comparison" description="Reserved for future agent judgments"/>{['Context agreement', 'Intent agreement', 'Emotion signal'].map(x => <div className="flex items-center justify-between border-t border-subtle py-3 first:border-0 first:pt-0" key={x}><span className="text-sm">{x}</span><span className="flex items-center gap-1 text-xs text-success"><Check size={14}/>Comparable</span></div>)}</Panel><Panel><SectionTitle title="Key difference" description="Automated insight placeholder"/><p className="text-sm leading-7 text-muted">The first report shows a stronger urgency pattern and greater variance in its timeline. Future explainability modules will surface supporting evidence here.</p></Panel></div></PageShell> }

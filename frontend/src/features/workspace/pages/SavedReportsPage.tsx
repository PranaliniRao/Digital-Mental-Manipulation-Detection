import { Download, Search, Star } from 'lucide-react'
import { Button, Input } from '../../../components/ui'
import { RiskPill } from '../components/AnalysisTable'
import { PageShell, Panel } from '../components/PageShell'
import { reports } from '../data/placeholderData'
export function SavedReportsPage() { return <PageShell title="Saved reports" description="Curated intelligence reports, ready for review or export."><div className="mb-5 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3.5 top-3 text-muted" size={16}/><Input className="pl-10" placeholder="Search saved reports"/></div><Button variant="secondary">All categories</Button></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{reports.map((report, index) => <Panel key={report.title} className="group flex min-h-60 flex-col"><div className="flex items-start justify-between"><span className="rounded-control bg-violet/10 px-3 py-1 text-[.65rem] font-mono uppercase tracking-[.1em] text-violet">Saved report</span><button aria-label="Favorite report"><Star size={17} className={index === 0 ? 'fill-violet text-violet' : 'text-muted'}/></button></div><h2 className="mt-7 text-lg font-semibold">{report.title}</h2><p className="mt-2 text-sm text-muted">{report.subtitle}</p><div className="mt-auto flex items-center justify-between pt-7"><div><RiskPill risk={report.risk}/><span className="ml-2 text-xs text-muted">{report.tag}</span></div><Button variant="ghost" size="sm"><Download size={14}/>Export</Button></div></Panel>)}</div></PageShell> }

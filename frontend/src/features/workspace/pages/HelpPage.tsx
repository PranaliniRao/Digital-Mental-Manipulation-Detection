import { BookOpen, LifeBuoy, Search } from 'lucide-react'
import { Button, Input } from '../../../components/ui'
import { PageShell, Panel } from '../components/PageShell'
export function HelpPage() { const articles = [['Getting started', 'Learn how to choose an analysis workflow and read risk signals.'], ['Understanding reports', 'Understand summary risk, confidence, and placeholder signal cards.'], ['Workspace controls', 'Manage preferences, saved reports, and account settings.']]; return <PageShell title="Help center" description="Guidance for working confidently with SignalGuard’s frontend workspace."><div className="relative mx-auto max-w-2xl"><Search className="absolute left-4 top-3.5 text-muted" size={17}/><Input className="pl-11" placeholder="Search help articles"/></div><div className="mt-8 grid gap-5 md:grid-cols-3">{articles.map(([title, detail]) => <Panel key={title}><BookOpen className="text-violet" size={20}/><h2 className="mt-5 text-base font-semibold">{title}</h2><p className="mt-2 text-sm leading-6 text-muted">{detail}</p><button className="mt-5 text-xs font-semibold text-violet">Read article →</button></Panel>)}</div><Panel className="mt-5 flex flex-col items-start justify-between gap-5 sm:flex-row sm:items-center"><div><p className="text-base font-semibold">Need help from the team?</p><p className="mt-1 text-sm text-muted">Support channels will be connected in a future release.</p></div><Button variant="secondary"><LifeBuoy size={15}/>Contact support</Button></Panel></PageShell> }

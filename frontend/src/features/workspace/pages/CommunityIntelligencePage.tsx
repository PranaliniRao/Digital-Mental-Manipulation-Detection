import { ArrowUpRight, Flame, Users } from 'lucide-react'
import { PageShell, Panel, SectionTitle } from '../components/PageShell'
import { categories } from '../data/placeholderData'
export function CommunityIntelligencePage() { return <PageShell eyebrow="Shared intelligence · placeholder" title="Community intelligence" description="An illustrative view of research signals and emerging manipulation patterns across a future opt-in community."><div className="grid gap-4 sm:grid-cols-3"><Panel><Users className="text-violet" size={20}/><p className="mt-5 text-3xl font-semibold">18.2k</p><p className="text-xs text-muted">Active researchers</p></Panel><Panel><Flame className="text-warning" size={20}/><p className="mt-5 text-3xl font-semibold">247</p><p className="text-xs text-muted">Signals this week</p></Panel><Panel><ArrowUpRight className="text-success" size={20}/><p className="mt-5 text-3xl font-semibold">+34%</p><p className="text-xs text-muted">Verified discoveries</p></Panel></div><div className="mt-5 grid gap-5 xl:grid-cols-[.9fr_1.5fr]"><Panel><SectionTitle title="Trending manipulation types"/>{categories.map((c, i) => <div className="flex items-center gap-3 border-t border-subtle py-3 first:border-0 first:pt-0" key={c.label}><span className="font-mono text-xs text-muted">0{i + 1}</span><span className="size-2 rounded-full" style={{ background: c.color }}/><span className="flex-1 text-sm">{c.label}</span><span className="text-xs text-muted">{c.value}%</span></div>)}</Panel><Panel><SectionTitle title="Research highlights" description="Recently verified placeholder discoveries"/>{[['Coordinated urgency language', 'A recurring urgency pattern across 12 public claims.'], ['Reused visual provenance', 'Image sequence shares a common misleading crop.'], ['Context fragmentation', 'Claim accuracy changes materially without source context.']].map(([title, detail]) => <article className="border-t border-subtle py-4 first:border-0 first:pt-0" key={title}><p className="text-sm font-semibold">{title}</p><p className="mt-1 text-sm text-muted">{detail}</p><button className="mt-2 text-xs font-semibold text-violet">View research <ArrowUpRight className="inline" size={13}/></button></article>)}</Panel></div></PageShell> }

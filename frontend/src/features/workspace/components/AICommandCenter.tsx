import { Activity, Bell, Bot, BookOpenCheck, Clock3, FileSearch, Gauge, GitCompareArrows, History, Image, Plus, ShieldCheck } from 'lucide-react'
import { motion } from 'framer-motion'
import { useEffect, useState } from 'react'
import { Link } from 'react-router-dom'
import { Avatar, Button } from '../../../components/ui'
import { useProfile } from '../../../providers/ProfileProvider'
import { activities } from '../data/placeholderData'
import { Panel } from './PageShell'

const services = [
  ['Vision service', 'Ready', 'success'], ['RoBERTa', 'Online', 'success'], ['Emotion model', 'Active', 'success'], ['Agent network', 'Operational', 'success'], ['Judge', 'Available', 'success'], ['Meta judge', 'Ready', 'success'], ['Report engine', 'Ready', 'success'],
] as const
const quickActions = [[Plus, 'New analysis', '/app/new-analysis'], [FileSearch, 'Analyze text', '/app/text-analysis'], [Image, 'Analyze image', '/app/image-analysis'], [History, 'History', '/app/history'], [BookOpenCheck, 'Saved reports', '/app/saved-reports'], [Gauge, 'Analytics', '/app/analytics'], [GitCompareArrows, 'Compare reports', '/app/compare-reports']] as const
const notifications = [['Analysis complete', 'Election claim thread is ready for review.', '2m ago'], ['Report generated', 'Market outlook newsletter was saved.', '1h ago'], ['Vision model ready', 'Interface status changed to operational.', 'Today'], ['System update', 'New explainability widgets are available.', 'Today']]

export function AICommandCenter() { const [now, setNow] = useState(() => new Date()); useEffect(() => { const timer = window.setInterval(() => setNow(new Date()), 30_000); return () => window.clearInterval(timer) }, []); const date = new Intl.DateTimeFormat('en-IN', { weekday: 'long', month: 'long', day: 'numeric' }).format(now); const time = new Intl.DateTimeFormat('en-IN', { hour: 'numeric', minute: '2-digit' }).format(now); const { fullName } = useProfile(); return <motion.section initial={{ opacity: 0, y: 14, scale: .99 }} animate={{ opacity: 1, y: 0, scale: 1 }} transition={{ duration: .45 }} className="space-y-5"><Panel className="relative overflow-hidden p-6 sm:p-7"><motion.span animate={{ y: [0, -9, 0], opacity: [.28, .62, .28] }} transition={{ duration: 7, repeat: Infinity }} className="pointer-events-none absolute -right-8 -top-8 size-44 rounded-full border border-violet/20"/><div className="relative flex flex-col justify-between gap-5 md:flex-row md:items-center"><div className="flex items-center gap-4"><Avatar name="AS" className="size-12 text-sm"/><div><p className="eyebrow">{date} · {time}</p><h1 className="mt-1 text-2xl font-semibold tracking-tight sm:text-3xl">Welcome back, {fullName}.</h1><p className="mt-1 text-sm text-muted">Your AI command center is healthy and ready for work.</p></div></div><div className="flex items-center gap-3"><span className="flex items-center gap-2 rounded-full border border-success/25 bg-success/10 px-3 py-1.5 text-xs font-semibold text-success"><span className="relative flex size-2"><span className="absolute inline-flex size-full animate-ping rounded-full bg-success/60"/><span className="relative inline-flex size-2 rounded-full bg-success"/></span>AI health 98.7%</span><Link to="/app/new-analysis"><Button><Plus size={15}/>New analysis</Button></Link></div></div></Panel><div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{services.map(([name, detail], index) => <motion.div key={name} initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .04 }} whileHover={{ y: -2 }}><Panel className="flex items-center gap-3 p-4"><span className="relative flex size-2"><span className="absolute inline-flex size-full animate-ping rounded-full bg-success/60"/><span className="relative inline-flex size-2 rounded-full bg-success"/></span><span className="flex-1 text-xs font-semibold">{name}</span><span className="font-mono text-[.6rem] uppercase text-success">{detail}</span></Panel></motion.div>)}</div><div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Panel><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-muted">Quick actions</p><div className="mt-5 grid gap-2 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{quickActions.map(([Icon, label, href], index) => <motion.div initial={{ opacity: 0, y: 6 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .18 + index * .035 }} key={label}><Link to={href} className="group flex items-center gap-3 rounded-control border border-subtle bg-canvas/40 p-3 transition hover:-translate-y-px hover:border-violet/35 hover:shadow-glow"><span className="grid size-8 place-items-center rounded-lg bg-violet/10 text-violet"><Icon size={15}/></span><span className="text-xs font-semibold">{label}</span></Link></motion.div>)}</div><div className="mt-6 grid gap-3 border-t border-subtle pt-5 sm:grid-cols-3"><HealthMetric icon={ShieldCheck} label="Overall AI health" value="98.7%" detail="Operational"/><HealthMetric icon={Activity} label="Backend status" value="Ready" detail="Mock interface state"/><HealthMetric icon={Clock3} label="Average response" value="1.2s" detail="Placeholder timing"/></div></Panel><Panel><div className="flex items-center justify-between"><div><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-muted">Notification center</p><p className="mt-1 text-sm font-semibold">Recent updates</p></div><Bell size={18} className="text-violet"/></div><div className="mt-4 space-y-1">{notifications.map(([title, detail, when], index) => <motion.article initial={{ opacity: 0, x: 8 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: .18 + index * .06 }} className="border-t border-subtle py-3 first:border-0 first:pt-0" key={title}><div className="flex gap-3"><span className="mt-1.5 size-1.5 shrink-0 rounded-full bg-violet"/><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><p className="text-xs font-semibold">{title}</p><span className="whitespace-nowrap text-[.62rem] text-muted">{when}</span></div><p className="mt-1 text-xs leading-5 text-muted">{detail}</p></div></div></motion.article>)}</div></Panel></div><div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]"><Panel><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-muted">Recent activity</p><div className="mt-4 grid gap-3 sm:grid-cols-2">{activities.slice(0, 4).map(([time, title, detail], index) => <motion.div initial={{ opacity: 0, y: 7 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .1 + index * .05 }} key={title} className="rounded-control border border-subtle bg-canvas/35 p-3"><p className="text-xs font-semibold">{title}</p><p className="mt-1 text-xs text-muted">{detail}</p><p className="mt-3 font-mono text-[.6rem] uppercase text-violet">{time}</p></motion.div>)}</div></Panel><Panel><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-muted">System health</p><div className="mt-5 space-y-4">{[['Analysis pipeline', 99], ['Report generation', 97], ['Workspace delivery', 100]].map(([label, value]) => <div key={label as string}><div className="mb-1.5 flex justify-between text-xs"><span>{label as string}</span><span className="text-success">{value}%</span></div><div className="h-1.5 overflow-hidden rounded-full bg-white/[.08]"><motion.div initial={{ width: 0 }} animate={{ width: `${value}%` }} transition={{ duration: .7, ease: 'easeOut' }} className="h-full rounded-full bg-violet"/></div></div>)}</div></Panel></div></motion.section> }

function HealthMetric({ icon: Icon, label, value, detail }: { icon: typeof ShieldCheck; label: string; value: string; detail: string }) { return <div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-lg bg-violet/10 text-violet"><Icon size={15}/></span><div><p className="text-[.65rem] text-muted">{label}</p><p className="text-sm font-semibold">{value} <span className="text-[.63rem] font-normal text-muted">· {detail}</span></p></div></div> }

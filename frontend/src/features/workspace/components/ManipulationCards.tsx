import { CheckCircle2, Circle } from 'lucide-react'
import { motion } from 'framer-motion'
import type { ExplainabilityModel } from '../data/explainabilityModel'
import { Panel, SectionTitle } from './PageShell'
export function ManipulationCards({ techniques }: { techniques: ExplainabilityModel['techniques'] }) { return <section><SectionTitle title="Manipulation technique explorer" description="Detected techniques and the evidence supporting them"/><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-4">{techniques.map((technique, index) => <motion.div initial={{ opacity: 0, y: 8 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} transition={{ delay: index * .035 }} key={technique.title}><Panel className="h-full p-4"><div className="flex items-center justify-between gap-3">{technique.detected ? <CheckCircle2 size={17} className="text-success"/> : <Circle size={17} className="text-muted"/>}<span className={`font-mono text-[.6rem] uppercase ${technique.detected ? 'text-violet' : 'text-muted'}`}>{technique.detected ? 'Detected' : 'Not detected'}</span></div><h3 className="mt-4 text-sm font-semibold">{technique.title}</h3><p className="mt-2 text-xs leading-5 text-muted">{technique.definition}</p><p className="mt-4 border-t border-subtle pt-3 text-xs text-violet">{technique.evidence}</p><p className="mt-2 font-mono text-[.6rem] text-muted">Confidence · {technique.confidence}%</p></Panel></motion.div>)}</div></section> }

import { Check, ChevronRight } from 'lucide-react'
import { motion } from 'framer-motion'
import type { ReasoningStep } from '../data/explainabilityModel'
import { Panel, SectionTitle } from './PageShell'
export function ReasoningTimeline({ steps, active = steps.length - 1, onSelect }: { steps: ReasoningStep[]; active?: number; onSelect?: (index: number) => void }) { return <Panel><SectionTitle title="Reasoning timeline" description="A traceable path from input to final decision"/><ol className="relative space-y-0">{steps.map((step, index) => <li key={step.title} className="relative flex gap-4 pb-5 last:pb-0"><span className="absolute left-[11px] top-6 h-[calc(100%-8px)] w-px bg-white/10 last:hidden"/><motion.button whileHover={{ x: 2 }} onClick={() => onSelect?.(index)} className="relative z-10 grid size-6 shrink-0 place-items-center rounded-full border border-violet/35 bg-violet/10 text-violet"><Check size={13}/></motion.button><button onClick={() => onSelect?.(index)} className="min-w-0 flex-1 text-left"><div className="flex items-center justify-between gap-3"><p className={`text-sm ${index === active ? 'font-semibold text-ink' : 'font-medium text-muted'}`}>{step.title}</p><span className="font-mono text-[.6rem] text-violet">{step.confidence}%</span></div><p className="mt-1 text-xs leading-5 text-muted">{step.detail}</p></button>{index === active && <ChevronRight className="mt-1 text-violet" size={16}/>}</li>)}</ol></Panel> }

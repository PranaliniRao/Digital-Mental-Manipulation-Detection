import { Sparkles } from 'lucide-react'
import { motion } from 'framer-motion'
import { Skeleton } from '../../../components/ui'
import { Panel } from './PageShell'
import { AnalysisProgressTimeline } from './AnalysisProgressTimeline'

export function AnalysisLoadingExperience({ activeStage }: { activeStage: number }) { return <div className="grid gap-5 xl:grid-cols-[1.25fr_.75fr]"><Panel className="relative overflow-hidden"><motion.span animate={{ scale: [1, 1.18, 1], opacity: [.5, 1, .5] }} transition={{ repeat: Infinity, duration: 2.2 }} className="absolute right-10 top-10 size-24 rounded-full bg-violet/15 blur-2xl"/><div className="relative"><span className="grid size-12 place-items-center rounded-control bg-violet/12 text-violet"><Sparkles size={22}/></span><div className="mt-7"><AnalysisProgressTimeline activeStage={activeStage}/></div></div></Panel><div className="space-y-5"><Panel><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-muted">Preparing report</p><div className="mt-5 space-y-4"><Skeleton className="h-5 w-3/5"/><Skeleton className="h-3 w-full"/><Skeleton className="h-3 w-5/6"/><Skeleton className="h-20 w-full"/></div></Panel><Panel><p className="text-sm font-semibold">Live analysis signals</p><p className="mt-2 text-xs leading-6 text-muted">Visual placeholders for future streamed model and agent activity.</p><div className="mt-4 flex gap-2">{[0, 1, 2, 3].map(i => <motion.span key={i} animate={{ y: [0, -5, 0], opacity: [.3, 1, .3] }} transition={{ repeat: Infinity, duration: .85, delay: i * .12 }} className="size-2 rounded-full bg-violet"/>)}</div></Panel></div></div> }

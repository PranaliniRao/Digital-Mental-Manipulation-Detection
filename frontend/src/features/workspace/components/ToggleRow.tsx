import { useState } from 'react'
export function ToggleRow({ title, description, initial = false }: { title: string; description: string; initial?: boolean }) { const [on, setOn] = useState(initial); return <div className="flex items-center justify-between gap-5 border-t border-subtle py-4 first:border-0 first:pt-0"><div><p className="text-sm font-medium">{title}</p><p className="mt-0.5 text-xs text-muted">{description}</p></div><button onClick={() => setOn(!on)} aria-pressed={on} className={`relative h-6 w-11 rounded-full transition ${on ? 'bg-violet' : 'bg-white/10'}`}><span className={`absolute top-1 size-4 rounded-full bg-white shadow transition ${on ? 'left-6' : 'left-1'}`}/></button></div> }

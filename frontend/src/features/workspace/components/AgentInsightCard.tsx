import { ChevronDown } from 'lucide-react'
import { motion, AnimatePresence } from 'framer-motion'
import { useState } from 'react'
import type { AgentInsight } from '../data/analysisMockData'
import { Panel } from './PageShell'

export function AgentInsightCard({ agent }: { agent: AgentInsight }) { const [open, setOpen] = useState(false); return <Panel className="p-5"><button className="w-full text-left" onClick={() => setOpen(!open)} aria-expanded={open}><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-semibold">{agent.name}</p><p className="mt-1 text-xs leading-5 text-muted">{agent.summary}</p></div><ChevronDown className={`shrink-0 text-violet transition-transform ${open ? 'rotate-180' : ''}`} size={17}/></div><div className="mt-4 flex items-center gap-3"><div className="h-1.5 flex-1 overflow-hidden rounded-full bg-white/[.08]"><motion.div initial={{ width: 0 }} whileInView={{ width: `${agent.confidence}%` }} viewport={{ once: true }} className="h-full rounded-full bg-violet"/></div><span className="font-mono text-[.62rem] text-muted">{agent.confidence}%</span></div></button><AnimatePresence>{open && <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} className="overflow-hidden"><div className="mt-4 border-t border-subtle pt-4"><p className="text-xs leading-6 text-muted">{agent.reasoning}</p><div className="mt-3 flex flex-wrap gap-2">{agent.evidence.map(item => <span key={item} className="rounded-full border border-violet/20 bg-violet/5 px-2.5 py-1 text-[.65rem] text-violet">{item}</span>)}</div><p className="mt-3 font-mono text-[.61rem] uppercase tracking-[.12em] text-muted">Risk contribution · {agent.contribution}%</p></div></motion.div>}</AnimatePresence></Panel> }

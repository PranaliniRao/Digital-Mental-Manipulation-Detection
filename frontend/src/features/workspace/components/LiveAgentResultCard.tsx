import { ChevronDown, CircleCheck } from 'lucide-react'
import { AnimatePresence, motion } from 'framer-motion'
import { useState } from 'react'
import type { LiveAgentResult } from '../data/normalizeAnalysisResult'
import { formatDisplayLabel } from '../../../utils/formatDisplayLabel'

export function LiveAgentResultCard({ agent, index }: { agent: LiveAgentResult; index: number }) { const [open, setOpen] = useState(false); return <motion.article initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .06 }} className="overflow-hidden rounded-control border border-violet/15 bg-canvas/35"><button onClick={() => setOpen(value => !value)} className="flex w-full items-center gap-3 p-3.5 text-left"><CircleCheck size={16} className="text-success"/><span className="min-w-0 flex-1"><span className="block text-xs font-semibold">{agent.name}</span><span className="mt-0.5 block truncate text-[.65rem] text-muted">{agent.summary}</span></span><span className="font-mono text-[.62rem] text-violet">{agent.confidence === null ? '—' : `${agent.confidence}%`}</span><ChevronDown size={15} className={`text-muted transition-transform ${open ? 'rotate-180' : ''}`}/></button><AnimatePresence>{open && <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} className="overflow-hidden border-t border-subtle"><div className="space-y-2 p-3.5 text-xs leading-5"><p className="text-muted">{agent.reasoning}</p><div className="flex justify-between font-mono text-[.6rem] uppercase tracking-[.08em]"><span className="text-muted">Status · {formatDisplayLabel(agent.status)}</span><span className="text-violet">Risk contribution · {agent.contribution === null ? '—' : `${agent.contribution}%`}</span></div></div></motion.div>}</AnimatePresence></motion.article> }

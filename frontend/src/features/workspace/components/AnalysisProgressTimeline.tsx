import { Check, Circle } from 'lucide-react'
import { motion } from 'framer-motion'
import { analysisStages } from '../data/analysisMockData'

export function AnalysisProgressTimeline({ activeStage }: { activeStage: number }) { const progress = Math.min(100, Math.round((activeStage / (analysisStages.length - 1)) * 100)); return <div><div className="mb-6 flex items-end justify-between"><div><p className="font-mono text-[.62rem] uppercase tracking-[.14em] text-violet">Analysis in progress</p><h2 className="mt-2 text-xl font-semibold">{analysisStages[activeStage]}</h2></div><span className="text-sm font-semibold text-violet">{progress}%</span></div><div className="h-1.5 overflow-hidden rounded-full bg-white/[.08]"><motion.div className="h-full rounded-full bg-gradient-to-r from-[#7c3aed] to-[#c084fc]" animate={{ width: `${progress}%` }} transition={{ duration: .45, ease: 'easeOut' }}/></div><ol className="mt-7 grid gap-3 sm:grid-cols-2">{analysisStages.map((stage, index) => { const complete = index < activeStage; const current = index === activeStage; return <motion.li key={stage} initial={{ opacity: 0, y: 5 }} animate={{ opacity: index <= activeStage + 1 ? 1 : .36, y: 0 }} className="flex items-center gap-3"><span className={`grid size-6 shrink-0 place-items-center rounded-full border ${complete ? 'border-violet bg-violet text-white' : current ? 'border-violet bg-violet/15 text-violet' : 'border-white/15 text-muted'}`}>{complete ? <Check size={13}/> : <Circle size={10} fill={current ? 'currentColor' : 'none'}/>}</span><span className={`text-xs ${current ? 'font-semibold text-ink' : 'text-muted'}`}>{stage}</span></motion.li> })}</ol></div> }

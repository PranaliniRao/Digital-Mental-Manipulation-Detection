import { Link } from 'react-router-dom'
import { Button, Input } from '../../../components/ui'
export function SignUpPage() { return <section><p className="eyebrow">Request access</p><h1 className="mt-4 tracking-display text-4xl font-semibold">Build a more resilient information environment.</h1><p className="mt-4 text-sm leading-6 text-muted">Account provisioning is intentionally outside this foundation phase.</p><form className="mt-10 space-y-5" onSubmit={(event) => event.preventDefault()}><label className="block text-xs font-semibold text-ink">Full name<Input className="mt-2" placeholder="Your name" /></label><label className="block text-xs font-semibold text-ink">Work email<Input className="mt-2" type="email" placeholder="you@company.com" /></label><label className="block text-xs font-semibold text-ink">Password<Input className="mt-2" type="password" placeholder="Create a secure password" /></label><Button className="w-full" size="lg" type="submit">Create account</Button></form><p className="mt-7 text-center text-xs text-muted">Already have access? <Link className="font-semibold text-cyan" to="/login">Sign in</Link></p></section> }

